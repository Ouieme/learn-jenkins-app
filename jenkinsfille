pipeline {
    agent {
        docker {
            image 'node:18-alpine'
            args '-v C:/ProgramData/Jenkins/.jenkins/workspace/pipeline-Vscode/:/workspace'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'cd /workspace && npm install'
            }
        }
    }
}
